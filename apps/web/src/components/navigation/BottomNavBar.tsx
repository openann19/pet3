import { Link, useLocation } from "react-router-dom";
const items=[{to:"/discover",label:"Discover",icon:"🧭"},{to:"/chat",label:"Chat",icon:"💬"},{to:"/matches",label:"Matches",icon:"❤️"},{to:"/adopt",label:"Adopt",icon:"🐾"},{to:"/community",label:"Community",icon:"👥"},{to:"/profile",label:"Profile",icon:"👤"}];
export default function BottomNavBar(){ const { pathname }=useLocation(); return(<nav className="fixed bottom-0 left-0 right-0 z-40 border-t border-black/10 bg-[rgba(20,20,24,0.8)] backdrop-blur-md md:hidden"><ul className="grid grid-cols-6">{items.map(it=>{const active=pathname.startsWith(it.to);return(<li key={it.to} className="text-center"><Link to={it.to} className={`block py-2 text-xs ${active?"font-bold":"opacity-70"}`}><div className="text-lg leading-none">{it.icon}</div>{it.label}</Link></li>)})}</ul></nav>); }
