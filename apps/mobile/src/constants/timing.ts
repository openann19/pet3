/**
 * Timing Constants
 * 
 * Shared timing constants used across the mobile app for delays, timeouts, and animations.
 * Location: apps/mobile/src/constants/timing.ts
 */

/**
 * Standard refresh delay for pull-to-refresh operations
 * Provides consistent UX feedback timing
 */
export const REFRESH_DELAY_MS = 500

/**
 * Standard debounce delay for user input
 */
export const DEBOUNCE_DELAY_MS = 300

/**
 * Standard throttle delay for scroll/resize events
 */
export const THROTTLE_DELAY_MS = 100

/**
 * Standard timeout for network requests
 */
export const NETWORK_TIMEOUT_MS = 30000

/**
 * Standard interval for periodic updates
 */
export const UPDATE_INTERVAL_MS = 1000

